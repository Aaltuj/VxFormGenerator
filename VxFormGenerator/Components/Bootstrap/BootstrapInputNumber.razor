@typeparam TValue
@inherits VxFormGenerator.Components.Bootstrap.BootstrapInputNumberComponent<TValue>
<!-- temporary workaround for bootstrap validation classes
     until  https://github.com/dotnet/aspnetcore/pull/24835 is accepted and merged-->
<InputNumber Value="@Value" ValueChanged="@ValueChanged" ValueExpression="@ValueExpression"
              class="@ValidationHelpers.FixClassNames(CssClass)"></InputNumber>
